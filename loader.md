<!DOCTYPE html> <html lang="en"> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <title>ZerovoLabs | Premium Experience</title> <style> @import url(''); :root { --curious-blue: #2899c6; --aztec: #050a0c; --gulf-stream: #77abab; --casal: #2a5b6e; --blue-dianne: #1b3843; --white: #ffffff; --glow: rgba(40, 153, 198, 0.4); } body, html { margin: 0; padding: 0; width: 100%; height: 100%; background-color: var(--aztec); font-family: 'Inter', sans-serif; overflow: hidden; display: flex; justify-content: center; align-items: center; } #loader-wrapper { position: fixed; inset: 0; z-index: 99999; background-color: var(--aztec); display: flex; flex-direction: column; justify-content: center; align-items: center; transition: opacity 1s cubic-bezier(0.4, 0, 0.2, 1), filter 1.2s cubic-bezier(0.4, 0, 0.2, 1), transform 1.2s cubic-bezier(0.8, 0, 0.2, 1); } /* Entrance Animation */ .brand-container { position: relative; display: flex; flex-direction: column; align-items: center; opacity: 0; transform: scale(0.96) translateY(10px); animation: reveal 1.2s cubic-bezier(0.2, 0.8, 0.2, 1) forwards 0.2s; } @keyframes reveal { to { opacity: 1; transform: scale(1) translateY(0); } } svg { width: clamp(280px, 80vw, 1000px); height: auto; overflow: visible; filter: drop-shadow(0 0 20px rgba(0,0,0,0.5)); } .text-base { fill: var(--blue-dianne); font-weight: 900; font-size: 130px; letter-spacing: -0.04em; opacity: 0.3; } .text-liquid { font-weight: 900; font-size: 130px; letter-spacing: -0.04em; filter: url(#glowFilter); /* Adds a soft bloom to the liquid */ } .loader-meta { position: absolute; bottom: -60px; width: 100%; display: flex; justify-content: space-between; align-items: center; font-family: ui-monospace, 'Cascadia Code', monospace; font-size: 10px; color: var(--casal); text-transform: uppercase; letter-spacing: 0.2em; padding: 0 5px; } .progress-container { display: flex; align-items: center; gap: 20px; flex-grow: 1; } .mini-track { flex-grow: 1; height: 1px; background: rgba(255, 255, 255, 0.05); position: relative; max-width: 120px; } .mini-bar { position: absolute; left: 0; top: 0; height: 100%; width: 0%; background: var(--white); box-shadow: 0 0 10px var(--white); transition: none; } .status-box { color: var(--white); opacity: 0.6; min-width: 200px; text-align: left; } .percent-box { color: var(--white); font-weight: 400; min-width: 45px; text-align: right; } /* Exit State */ .loaded #loader-wrapper { opacity: 0; filter: blur(20px); transform: scale(1.05); pointer-events: none; } .bottom-pill { position: absolute; bottom: 40px; width: 30px; height: 2px; background: var(--casal); border-radius: 10px; opacity: 0.2; } </style> </head> <body> <div id="loader-wrapper"> <div class="brand-container"> <svg viewBox="0 0 900 200"> <defs> <linearGradient id="liquidGradient" x1="0%" y1="0%" x2="0%" y2="100%"> <stop offset="0%" style="stop-color:var(--curious-blue);stop-opacity:1" /> <stop offset="100%" style="stop-color:var(--gulf-stream);stop-opacity:1" /> </linearGradient> <!-- Bloom Filter --> <filter id="glowFilter" x="-20%" y="-20%" width="140%" height="140%"> <feGaussianBlur in="SourceGraphic" stdDeviation="2" result="blur" /> <feComposite in="SourceGraphic" in2="blur" operator="over" /> </filter> <mask id="waveMask"> <path id="wave-path" d="" fill="white" /> </mask> </defs> <text x="50%" y="50%" dominant-baseline="middle" text-anchor="middle" class="text-base">ZerovoLabs</text> <text x="50%" y="50%" dominant-baseline="middle" text-anchor="middle" class="text-liquid" fill="url(#liquidGradient)" mask="url(#waveMask)">ZerovoLabs</text> </svg> <div class="loader-meta"> <div class="status-box"> <span id="status-text">Initializing</span> </div> <div class="progress-container"> <div class="mini-track"> <div class="mini-bar" id="progress-bar"></div> </div> <div class="percent-box"> <span id="percent-num">0</span>% </div> </div> </div> </div> <div class="bottom-pill"></div> </div> <script> const wavePath = document.getElementById('wave-path'); const progressBar = document.getElementById('progress-bar'); const percentNum = document.getElementById('percent-num'); const statusText = document.getElementById('status-text'); const wrapper = document.getElementById('loader-wrapper'); let currentProgress = 0; let targetProgress = 0; let time = 0; const systemMessages = [ { threshold: 0, text: "System Boot" }, { threshold: 15, text: "Kernel.sync(assets)" }, { threshold: 40, text: "Neural.init(core)" }, { threshold: 65, text: "UI.render(shell)" }, { threshold: 85, text: "Build.finalize()" } ]; function animate() { // Organic Loading Curve: // We increment targetProgress randomly, but we LERP currentProgress towards it // for buttery smooth movement. if (targetProgress < 100) { let increment = Math.random() * 0.5; if (targetProgress > 30 && targetProgress < 45) increment *= 0.2; // Pause for "effect" targetProgress += increment; } // Smoothing (LERP) currentProgress += (targetProgress - currentProgress) * 0.08; time += 0.06; if (currentProgress >= 99.9) { currentProgress = 100; updateUI(100); setTimeout(finish, 800); return; } updateUI(currentProgress); requestAnimationFrame(animate); } function updateUI(p) { const width = 900; const height = 200; // 1. Smooth Wave Surface const yThreshold = height - (p / 100) * height; let d = `M 0 ${height} L 0 ${yThreshold} `; // Refined Wave Physics const waveFrequency = 0.015; const waveAmplitude = 6; for (let x = 0; x <= width; x += 10) { // Wave calms down as it reaches the very top const calmFactor = Math.sin((p / 100) * Math.PI); const waveY = Math.sin((x * waveFrequency) + time) * (waveAmplitude * calmFactor); d += `L ${x} ${yThreshold + waveY} `; } d += `L ${width} ${yThreshold} L ${width} ${height} Z`; wavePath.setAttribute('d', d); // 2. Linear Progress Bar progressBar.style.width = p + '%'; // 3. Counter with decimals for high-end feel (optional, but using floor for simplicity) percentNum.innerText = Math.floor(p); // 4. Stat